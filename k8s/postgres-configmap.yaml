apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-script
  namespace: stratroom-prod
data:
  init-db.sql: |
    CREATE DATABASE stratroomlicense;
    \c clean;
    CREATE SCHEMA IF NOT EXISTS orgstructure;
