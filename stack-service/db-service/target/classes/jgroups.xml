<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="urn:org:jgroups" xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/JGroups-4.0.xsd">


	<TCP bind_port="${jgroups.tcp.port:7800}" bind_addr="${jgroups.tcp.address:localhost}"
		send_buf_size="640k" sock_conn_timeout="300" thread_naming_pattern="p1" enable_diagnostics="false"
		thread_pool.min_threads="${jgroups.thread_pool.min_threads:2}"
		thread_pool.max_threads="${jgroups.thread_pool.max_threads:30}"
		thread_pool.keep_alive_time="60000" log_discard_msgs="true" />

	<TCPPING async_discovery="true"
		initial_hosts="${jgroups.tcpping.initial_hosts:localhost[7800],localhost[7801]}"
		port_range="0" />


	<MERGE3 min_interval="10000" max_interval="30000" />

	<FD_SOCK />

	<FD_ALL timeout="9000" interval="3000" timeout_check_interval="5000" />

	<VERIFY_SUSPECT timeout="1500" />

	<BARRIER />

	<pbcast.NAKACK2 use_mcast_xmit="false" xmit_interval="1000"
		xmit_table_num_rows="50" xmit_table_msgs_per_row="1024"
		xmit_table_max_compaction_time="30000" resend_last_seqno="true" />

	<UNICAST3 xmit_interval="500" xmit_table_num_rows="50"
		xmit_table_msgs_per_row="1024" xmit_table_max_compaction_time="30000"
		conn_expiry_timeout="0" />

	<pbcast.STABLE stability_delay="500" desired_avg_gossip="50000"
		max_bytes="1M" />

	<pbcast.GMS print_local_addr="true" join_timeout="2000" />

	<MFC max_credits="2M" min_threshold="0.40" />

	<FRAG2 />
</config>